namespace Enum {
    export enum GameStatus {
        /**
         * 默认
         */
        DEFAULT = 0,
        /**
         * 猜大小
         */
        GUESS = 1,
        /**
         * 进行中
         */
        EXECUTE = 2,
    }
}