/**
 * 工具类-投注结果转换
 */
var Utils;
(function (Utils) {
    //投注类型
    var BetPos = (function () {
        function BetPos() {
        }
        /**
         * 投注类型结果转换
         */
        BetPos.transform = function (type) {
            var language = new LanguageUtils.Language();
            var result;
            return result;
        };
        return BetPos;
    }());
    Utils.BetPos = BetPos;
})(Utils || (Utils = {}));
//# sourceMappingURL=BetPos.js.map