/**
 * 工具类-投注结果转换
 */
namespace Utils {
    //投注类型
    export class BetPos {
        /**
         * 投注类型结果转换
         */
        static transform(type: number): string {
            let language: LanguageUtils.Language = new LanguageUtils.Language();
            let result: string;

            return result;
        }
    }
}